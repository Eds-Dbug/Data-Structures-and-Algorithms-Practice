class DynamicArr {
    constructor() {
        this.length = 0;
        this.maxCapacity = 1;
        this.array = new Array(this.maxCapacity);
    }

    addElement() {

    }

    deleteElement() {
        
    }

    resize() {
        this.array = new Array(this.maxCapacity * 2);
    }

    getIndex(i) {

    }
}